@{
  Layout = "_Layout";
}

@model UniversityRegistrar.Models.Course

<h4>Add a new task</h4>

@using (Html.BeginForm())
{
    @Html.LabelFor(model => model.CourseName)
    @Html.TextBoxFor(model => model.CourseName)<br>

    @Html.LabelFor(model => model.CourseNumber)
    @Html.TextBoxFor(model => model.CourseNumber)<br>

    @Html.Label("Select student")
    @Html.DropDownList("StudentId")<br>

    <input type="submit" value="Add new Course" />
}
<p>@Html.ActionLink("Show all Courses", "Index")</p>